# .NET
bin/
obj/
*.user
*.suo
.vs/

# EF Core local DBs
*.db
*.db-shm
*.db-wal

# Node
node_modules/
dist/
.vite/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/

# Environment
.env
.env.local
.env.*.local

# Local production build output (created by scripts/deploy.sh)
/api-publish/

# macOS / cloud-sync duplicate artifacts ("Program 2.cs", "CRM 2.sln", "Dockerfile 2").
# 224 of these once appeared next to the real files and broke the build with duplicate-class
# errors; they must never be committed either.
* 2.*
* 2
*\ 2.*
